#include <stdio.h>

/**
 * main - Entry point
 *
 * print numbers base 10 from 0 using putchar
 *
 * Return: Always (0) Success 
 */
int main(void)
{
	int y;
	for (y = 0; y < 10; y++)
	putchar(y + '0');
	putchar('\n');

return (0);
}
