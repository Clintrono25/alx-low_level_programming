#include <stdio.h>
/**
 * main - Entry point
 *
 * print numbers of base 10 from 0
 *
 * Return: Always (0) Success
 */

int main(void)
{
	int x = 0;
	for (x = 0; x < 10; x++)
       {
	       printf("%i", x);
	       putchar('\n');
       }       
return (0);
}
