#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char x;
	
	for(int x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
		putchar(\n);
	}
	
return (0);
}

